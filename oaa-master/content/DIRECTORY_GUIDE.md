# Content 目录结构说明

本目录包含 Open Angel Arena 的所有游戏资源源文件，这些文件经过 Dota 2 工具编译后用于游戏运行。

---

## 📁 目录结构概览

```
content/
├── maps/                   # 地图源文件
│   ├── prefabs/           # 预制体
│   └── tilesets/          # 瓦片集
├── materials/              # 材质和纹理
│   ├── fires/             # 火焰材质
│   ├── ground/            # 地面材质
│   ├── models/            # 模型材质
│   ├── overviews/         # 地图预览材质
│   ├── particles/         # 粒子材质
│   └── vgui/              # UI 材质
├── models/                 # 3D 模型源文件
├── panorama/               # UI 界面资源
│   ├── images/            # UI 图片资源
│   ├── layout/            # UI 布局文件 (.xml)
│   ├── scripts/           # UI 脚本 (.js)
│   └── styles/            # UI 样式 (.css)
├── particles/              # 粒子特效
├── soundevents/            # 音效事件定义
└── sounds/                 # 音频文件
```

---

## 📁 maps/ - 地图源文件

存放游戏地图的 Valve Map Format (`.vmap`) 源文件。

### 地图文件列表

| 文件 | 说明 |
|------|------|
| `oaa_seasonal.vmap` | 标准 5v5 赛季地图（默认地图） |
| `oaa_alternate.vmap` | 6v6 替代模式地图 |
| `oaa_bigmode.vmap` | 10v10 大型对战地图 |
| `oaa_legacy.vmap` | 传统模式地图 |
| `tinymode.vmap` | 4v4 小型模式地图 |
| `tiny_legacy.vmap` | 小型传统地图 |
| `10v10.vmap` | 10v10 地图 |

### 子目录

| 目录 | 说明 |
|------|------|
| `prefabs/` | 可复用的地图预制体组件 |
| `tilesets/` | 地形瓦片集定义 |

### 使用 Hammer Editor

地图文件使用 Dota 2 的 Hammer Editor 编辑：
1. 打开 Dota 2 Workshop Tools
2. 启动 Hammer Editor
3. 打开 `.vmap` 文件进行编辑
4. 编译地图生成 `.vpk` 文件

---

## 📁 materials/ - 材质和纹理

游戏使用的材质和纹理文件。

### 子目录

| 目录 | 说明 |
|------|------|
| `fires/` | 火焰和燃烧效果材质 |
| `ground/` | 地面纹理和材质 |
| `models/` | 3D 模型使用的材质 |
| `overviews/` | 小地图和地图预览图材质 |
| `particles/` | 粒子特效使用的材质 |
| `vgui/` | UI 界面使用的材质 |

### 文件格式

- `.vmat` - Valve Material 文件（材质定义）
- `.vtex` - Valve Texture 文件（纹理源）
- `.tga` / `.png` - 纹理图片源文件

---

## 📁 models/ - 3D 模型

游戏使用的 3D 模型源文件。

### 包含内容

- 自定义英雄模型
- 自定义单位模型
- Boss 模型
- 建筑物模型
- 物品模型
- 特效模型

### 文件格式

- `.vmdl` - Valve Model 文件（模型定义）
- `.dmx` - 模型源文件
- `.fbx` - 通用 3D 模型格式（导入用）
- `.smd` - Source Model 格式

### 模型工作流

1. 使用 3D 建模软件（Blender、Maya 等）创建模型
2. 导出为 `.fbx` 或 `.dmx` 格式
3. 在 Dota 2 Workshop Tools 中导入并编译

---

## 📁 panorama/ - UI 界面资源

Panorama UI 框架资源文件，用于构建游戏自定义界面。

### 子目录详解

#### images/ - UI 图片资源

存放 UI 使用的所有图片文件：
- 图标
- 按钮背景
- 边框装饰
- 技能图标
- 物品图标
- 其他 UI 元素

**支持格式**：`.png`, `.jpg`, `.psd`

#### layout/ - UI 布局文件

XML 格式的界面布局定义文件。

**文件格式**：`.xml`

**示例结构**：
```xml
<root>
    <Panel class="MainPanel">
        <Label text="Hello World" />
        <Button id="MyButton" />
    </Panel>
</root>
```

#### scripts/ - UI 脚本

JavaScript 脚本文件，处理 UI 交互逻辑。

**文件格式**：`.js`

**示例代码**：
```javascript
"use strict";

function OnButtonClicked() {
    $.Msg("Button was clicked!");
    // 处理按钮点击
}

(function() {
    $("#MyButton").SetPanelEvent("onactivate", OnButtonClicked);
})();
```

#### styles/ - UI 样式文件

CSS 样式文件，定义 UI 元素的外观。

**文件格式**：`.css`

**示例样式**：
```css
.MainPanel {
    width: 100%;
    height: 100%;
    background-color: #000000aa;
}

.Button {
    background-color: #ff5500;
    border-radius: 5px;
    padding: 10px;
}
```

---

## 📁 particles/ - 粒子特效

游戏使用的所有粒子特效文件。

### 子目录

| 目录 | 说明 |
|------|------|
| `abilities/` | 技能粒子特效 |
| `beastmaster_hawk/` | 兽王鹰特效 |
| `bosses/` | Boss 特效 |
| `capture_point_ring/` | 占领点环形特效 |
| `charger/` | 冲锋者特效 |
| `hero/` | 英雄相关特效 |
| `items/` | 物品特效 |
| `leshrac/` | 拉席克特效 |
| `magma_boss/` | 岩浆 Boss 特效 |
| `map/` | 地图环境特效 |
| `misc/` | 杂项特效 |
| `shielder/` | 盾卫者特效 |
| `test_particle/` | 测试粒子 |
| `ui_mouseactions/` | 鼠标操作 UI 特效 |
| `units/` | 单位特效 |
| `warning/` | 警告特效 |

### 根目录粒子文件

| 文件 | 说明 |
|------|------|
| `fountain_lazor*.vpcf` | 泉水激光特效系列 |
| `ghost_frostbite*.vpcf` | 幽灵冰霜特效 |
| `lycanboss_ruptureball*.vpcf` | 狼人 Boss 爆裂球特效 |
| `darkmoon_creep_warning*.vpcf` | 暗月小兵警告特效 |
| `dungeon_overhead_timer*.vpcf` | 地下城计时器特效 |

### 文件格式

- `.vpcf` - Valve Particle System 文件

### 粒子编辑

使用 Dota 2 Workshop Tools 的 Particle Editor：
1. 打开 Particle Editor
2. 创建或编辑 `.vpcf` 文件
3. 调整发射器、渲染器、操作器等属性
4. 保存并测试

---

## 📁 soundevents/ - 音效事件

音效事件定义文件，将音频文件与游戏事件关联。

### 文件格式

`.vsndevts` - Valve Sound Events 文件

### 音效事件示例

```
"soundevents/custom_sounds.vsndevts"
{
    "Hero.CustomAbility"
    {
        "type"              "dota_src1_3d"
        "vsnd_files"        "sounds/custom/ability_cast.vsnd"
        "volume"            "1.0"
        "pitch"             "1.0"
    }
}
```

---

## 📁 sounds/ - 音频文件

游戏使用的音频文件。

### 支持格式

- `.mp3` - 音乐和语音
- `.wav` - 音效
- `.vsnd` - Valve Sound 格式（编译后）

### 内容类型

- 背景音乐
- 技能音效
- UI 音效
- 环境音效
- 语音文件

---

## 🔧 开发指南

### 添加新粒子特效

1. 打开 Particle Editor
2. 创建新的 `.vpcf` 文件
3. 设计粒子系统
4. 在 Lua 脚本中引用：
   ```lua
   ParticleManager:CreateParticle("particles/my_effect.vpcf", PATTACH_WORLDORIGIN, nil)
   ```

### 添加新 UI 界面

1. 在 `panorama/layout/` 创建 `.xml` 布局文件
2. 在 `panorama/styles/` 创建对应 `.css` 样式
3. 在 `panorama/scripts/` 创建 `.js` 脚本（如需要）
4. 在 `panorama/images/` 添加所需图片资源

### 添加新音效

1. 将音频文件放入 `sounds/` 目录
2. 在 `soundevents/` 创建音效事件定义
3. 在游戏中使用 `EmitSoundOn()` 播放

### 添加新材质

1. 准备纹理图片（`.tga` 或 `.png`）
2. 在 `materials/` 创建 `.vmat` 材质文件
3. 编译材质

---

## 📚 相关文档

- [Game 目录详解](../game/DIRECTORY_GUIDE.md)
- [项目主文档](../README_CN.md)
- [Panorama UI 文档](../docs/panorama/)
- [粒子特效文档](../docs/particles/)
- [Valve Developer Wiki](https://developer.valvesoftware.com/wiki/Dota_2_Workshop_Tools)

---

## 🔗 资源工具

- **Hammer Editor** - 地图编辑器
- **Particle Editor** - 粒子特效编辑器
- **Material Editor** - 材质编辑器
- **Model Editor** - 模型查看和编辑器
- **Sound Event Editor** - 音效事件编辑器

所有工具都包含在 **Dota 2 Workshop Tools** 中，可通过 Steam 免费下载。

---

*本文档用于说明 Open Angel Arena 的 content 目录结构*
